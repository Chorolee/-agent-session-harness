# Routing Index

Routing-only index for the portable harness repository.

- Start with `AGENTS.md` for shared contract.
- Use `docs/public-contract.md` for the public launch and resume contract.
- Use `docs/release-model.md` for beta-stage release expectations.
- Use `docs/migration-guide.md` for alpha-to-beta upgrade guidance.
- Use `docs/ops/agent-operations.md` for the operating model.
- Use `tools/harness/README.md` for module-level runtime notes.
