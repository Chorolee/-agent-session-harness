# Routing Index

Routing-only index for the sample consumer workspace.

- Start with `AGENTS.md` for shared workspace rules.
- Use `docs/specs/project-roadmap/decision-log.md` for current decisions.
- Use `docs/specs/task-spec.md` for the active worker-launch spec.
