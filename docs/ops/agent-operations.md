# Agent Operations

Canonical operating index for this portable harness.

- Read `AGENTS.md` first.
- Use `operating-protocol.md` for head/worker flow.
- Use `session-packets.md` for packet fields.
- Use `resume-policy.md` for resume semantics.
- Use `worktree-drift.md` for worktree rules.
- Use `review-policy.md` and `model-routing.md` for review and routing guidance.
